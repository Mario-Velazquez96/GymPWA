# Current session

## Feature in progress
(none — all 8 SDD features 00–07 are DONE)

## State
2026-07-20: `07_pwa_install_and_cache` closed — the **last** feature. Full PWA
polish: complete manifest ("Rutinas Gym" / "Rutinas", standalone, `#0f172a`
theme/bg, 192/512/512-maskable icons), iOS `apple-touch-icon` + meta tags, white
dumbbell icon set generated by a no-new-dependency Node rasterizer, Workbox shell
precache + one CacheFirst `exercise-media` rule for Supabase Storage media (no
`/rest/`//`/auth/` caching — verified absent in the built `sw.js`), and a Spanish
README install section. 11/11 e2e green, reviewer APPROVE — see
`progress/history.md`.

**Project complete:** all features 00–07 are `done` in `feature_list.json`.

## Notes / blockers (remaining open items — all external/human, none block the build)
- **(a) Human iPhone PWA checklist (NOT auto-verifiable):** on a real iPhone with
  the app served over HTTPS — install to home screen, standalone open, correct
  icon, airplane-mode shell + cached GIF, Lighthouse "installable". Checklist in
  `progress/impl_07_pwa_install_and_cache.md`.
- **(b) Cross-repo seed:** the `Gym` repo must seed the `exercises` catalog +
  media and upload a real plan. Until then the app runs on
  `e2e/fixtures/test-plan.sql` ('Plan de prueba E2E', placeholder media).
- **(c) RLS own-insert check:** re-run `node scripts/check-rls.mjs` after (b) to
  complete the own-`user_id` insert check (currently SKIP — FK on `exercise_id`
  against an empty catalog).
